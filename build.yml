platform: linux

image_resource:
  type: docker-image
  source: {repository: busybox}

image_resource:
  type: docker-image
  source: {repository: node}

inputs:
- name: src-voice

run:
  path: ./src-voice/build
